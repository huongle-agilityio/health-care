import { Metadata } from 'next';

// Components
import { UpComing } from '@/sections';

export const metadata: Metadata = {
  title: 'Health Blog',
};

const Page = () => <UpComing />;

export default Page;
