import { Metadata } from 'next';

// Components
import { UpComing } from '@/ui/sections';

export const metadata: Metadata = {
  title: 'Health Blog',
};

const Page = () => <UpComing />;

export default Page;
